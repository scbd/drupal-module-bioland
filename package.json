{
    "name": "bioland",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "test": "jest",
        "test:ci": "jest --ci --coverage"
    },
    "devDependencies": {
        "jest": "29.7.0"
    }
}
